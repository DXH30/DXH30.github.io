# DXH30.github.io
Team Name : G.E.M.P.A
Occupation : Team Leaders

## Intro
Input your Challenge Here
This blog or page purpose is to share Battle Code, CTF, and Competitive Programming like Challenge Write Ups.

## Purpose pseudecode
```c++
typedef struct {
  void search() { /* Search for challenges */ }
  bool complete() { /* Check completion status */ }
  bool cancel() { /* Check cancel status */ }
  bool failed() { /* Check failed status */ }
  void reset();
  size_tt *writeup;
  void complete() {push(writeup,collections);}
} Challenge;

typedef struct {
  int level;
  bool goodness;
} Response;

void work(Challenge *collections) {
  int i = blog.getlastentry();
  Response thanks = {5,true};
  Response bad = {1,false};
  collections->search();
  if (collections->complete()) {
    blog.post(collections->complete());
  } else if (collections->cancel()) {
    blog.post((*other+i));
  } else {
    blog.posts.pop();
  }
  i++;
  collections->reset();
  return;
}
```

# Thanks
